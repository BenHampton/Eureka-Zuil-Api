server:
  port: 0

spring:
  application:
    name: rest-server

eureka:
  client:
    service-url:
      defaultzone: http://localhost:8671/eureka
  instance:
    prefer-ip-address: true

app:
  id: ${random.uuid}